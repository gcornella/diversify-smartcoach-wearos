<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- Wear OS triggers BOOT_COMPLETED or LOCKED_BOOT_COMPLETED after the watch reboots (e.g., after charging from 0%). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.BODY_SENSORS_BACKGROUND" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- Request specific foreground service permission -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">


        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:taskAffinity="">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"/>

        <activity android:name=".cloud.CloudStorageActivity"
            android:taskAffinity="" />

        <!-- Exercise Activity -->
        <activity
            android:name=".ExerciseActivity"
            android:exported="false"
            android:label="Exercise"
            android:taskAffinity=""
            android:launchMode="singleTop">
        </activity>

        <!-- Some devices only call LOCKED_BOOT_COMPLETED if storage is encrypted. Adding this and the BootReceiver ensures coverage. -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true">
        <intent-filter>
            <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            <action android:name="android.intent.action.BOOT_COMPLETED" />
            <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            <action android:name="android.intent.action.USER_UNLOCKED"/>
            </intent-filter>
        </receiver>

        <!-- Restart Receiver AlarmManager -->
        <receiver
            android:name=".receivers.RestartReceiver"
            android:enabled="true"
            android:exported="false"
            android:directBootAware="true" />

        <!-- Not worn -->
        <receiver android:name=".receivers.NotWornAlarmReceiver" android:exported="true" />

        <!-- Foreground Service: only for Android Version <= 14 write: -->
        <service
            android:name=".ForegroundSensorService"
            android:foregroundServiceType="health">
        </service>

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <!-- Complication Service -->
        <service
            android:name=".complications.MyComplicationProviderService"
            android:exported="true"
            android:enabled="true"
            android:label="Kurtosis Emoji"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER"
            android:directBootAware="true">

            <!-- Legacy action (some pickers still probe this) -->
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>

            <!-- Legacy metadata (safe to include for discovery) -->
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="0" />
        </service>

        <!-- Complication Restart -->
        <service
            android:name=".complications.MyServiceAliveCheckComplicationProviderService"
            android:exported="true"
            android:enabled="true"
            android:label="Kurtosis Restart"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER"
            android:directBootAware="true">

            <!-- Legacy action (some pickers still probe this) -->
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>

            <!-- Legacy metadata (safe to include for discovery) -->
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="0" />
        </service>

        <!-- Complication Data Source (hybrid: AndroidX + legacy for compatibility) -->
        <service
            android:name=".complications.MyProgressComplicationProviderService"
            android:exported="true"
            android:enabled="true"
            android:label="Kurtosis Progress"
            android:directBootAware="true"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">

            <!-- Legacy action (some pickers still probe this) -->
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>

            <!-- Legacy metadata (safe to include for discovery) -->
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="RANGED_VALUE,SHORT_TEXT" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="0" />
        </service>

        <!-- Complication Data Source (hybrid: AndroidX + legacy for compatibility) -->
        <service
            android:name=".complications.MyWearTimeComplicationProviderService"
            android:exported="true"
            android:enabled="true"
            android:label="Kurtosis WearTime"
            android:directBootAware="true"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">

            <!-- Legacy action (some pickers still probe this) -->
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>

            <!-- Legacy metadata (safe to include for discovery) -->
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="RANGED_VALUE" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="0" />
        </service>
    </application>
</manifest>